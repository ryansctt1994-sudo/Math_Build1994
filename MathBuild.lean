import MathBuild.IrrationalSqrt2

/-!
# MathBuild

Root module for the MathBuild library. Keeping the library entry point explicit
allows `lake build MathBuild` to discover and validate every public module.
-/
